#include<stdio.h>
#include<conio.h>
void main()
{
	float pie = 3.14;
    int radius;

    printf("Area Of The Circle:");
    scanf("%d",&radius);

    printf("Area Of Circle is %d\n",radius);

    float area = (float) (area = area * radius * radius);
    printf("The area of circle given is %f",area);

    getch();
}
